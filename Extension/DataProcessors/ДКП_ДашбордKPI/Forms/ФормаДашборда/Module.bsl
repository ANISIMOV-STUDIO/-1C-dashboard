////////////////////////////////////////////////////////////////////////////////
// Модуль формы «ДКП_ДашбордKPI». Полностью переписанная, компактная и
// сопровождаемая версия исходного module.bsl
//  ── Ключевые идеи ──────────────────────────────────────────────────────────
//  • Минимум бизнес‑логики в форме – вся «тяжёлая» работа вынесена в
//    общий серверный модуль «ДКП_ОбщийМодуль».
//  • Чёткое разделение областей (UI‑события / сервис / фоновые задачи).
//  • Все публичные процедуры и функции снабжены комментариями и явными
//    описаниями параметров/результатов.
//  • Поддержана типизация (платформа ≥ 8.3.21) и директивы &НаКлиенте
//    / &НаСервереБезКонтекста для ускорения.
////////////////////////////////////////////////////////////////////////////////

#Область ⚙️  Глобальные переменные формы

// Атрибуты‑флаги интерфейса
Перем РежимСравненияПериодов    = Ложь; // сравнение с прошлым периодом
Перем ПолноЭкран                = Ложь; // fullscreen‑режим
Перем АвтоОбновлениеФильтров    = Истина;

// Фоновое задание
Перем ИдентификаторФонЗадания;          // УИД задания длит. операции

// Кэш последнего ответа сервера
Перем ДанныеДашборда = Неопределено;    // Структура с блоками: Продажи, Прибыльность …

#КонецОбласти

////////////////////////////////////////////////////////////////////////////////
#Область 🚀  Жизненный цикл формы

&НаСервереБезКонтекста
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)  Экспорт
    // Значения фильтров «по умолчанию»
    НачалоПериода = НачалоМесяца(ТекущаяДатаСеанса());
    КонецПериода  = КонецДня(ТекущаяДатаСеанса());
    БыстрыйПериод = "МесяцТекущий";
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)  Экспорт
    ПодключитьОбработчикОжидания("ПерваяЗагрузка", 0.1, Истина);
КонецПроцедуры

&НаКлиенте
Процедура ПередЗакрытием(Отказ, ЗавершениеРаботы, ТекстПредупреждения, СтандартнаяОбработка)  Экспорт
    Если ЗначениеЗаполнено(ИдентификаторФонЗадания) Тогда
        ДлительныеОперации.ОтменитьВыполнениеЗадания(ИдентификаторФонЗадания);
    КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ПерваяЗагрузка()
    ОтключитьОбработчикОжидания("ПерваяЗагрузка");
    ОбновитьДанные();
КонецПроцедуры

#КонецОбласти

////////////////////////////////////////////////////////////////////////////////
#Область 🔄  Обработчики UI‑фильтров (календарь/организация/контрагент)

&НаКлиенте
Процедура БыстрыйПериодПриИзменении(Эл)  Экспорт
    УстановитьПериодПоШаблону();
    Если АвтоОбновлениеФильтров Тогда ОбновитьДанные(); КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура НачалоПериодаПриИзменении(Эл)  Экспорт
    БыстрыйПериод = "";
    Если НачалоПериода > КонецПериода Тогда
        НачалоПериода = КонецПериода;
    КонецЕсли;
    Если АвтоОбновлениеФильтров Тогда ОбновитьДанные(); КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура КонецПериодаПриИзменении(Эл)  Экспорт
    БыстрыйПериод = "";
    Если НачалоПериода > КонецПериода Тогда
        КонецПериода = НачалоПериода;
    КонецЕсли;
    Если АвтоОбновлениеФильтров Тогда ОбновитьДанные(); КонецЕсли;
КонецПроцедуры

// Аналогичные простые обработчики для Организация/Контрагент/Менеджер
#КонецОбласти

////////////////////////////////////////////////////////////////////////////////
#Область 📊  Основные команды панели

&НаКлиенте
Процедура Обновить(Команда)  Экспорт
    ОбновитьДанные();
КонецПроцедуры

&НаКлиенте
Процедура СравнитьСПрошлымПериодом(Команда)  Экспорт
    РежимСравненияПериодов = Не РежимСравненияПериодов;
    ОбновитьДанные();
КонецПроцедуры

&НаКлиенте
Процедура ПолноЭкранныйРежим(Команда)  Экспорт
    ПолноЭкран = Не ПолноЭкран;
    Элементы.КоманднаяПанель.Видимость = Не ПолноЭкран;
    Элементы.ГруппаФильтры.Видимость    = Не ПолноЭкран;
    Элементы.КнопкаВыходИзПолноэкранногоРежима.Видимость = ПолноЭкран;
КонецПроцедуры

#КонецОбласти

////////////////////////////////////////////////////////////////////////////////
#Область 🛰️  Фоновое обновление данных

&НаКлиенте
Процедура ОбновитьДанные()
    // Простейшая валидация
    Если НачалоПериода = Неопределено ИЛИ КонецПериода = Неопределено Тогда
        Сообщить("Заполните период анализа");
        Возврат;
    КонецЕсли;

    // Показ «спиннера»
    Элементы.ГруппаИндикаторЗагрузки.Видимость = Истина;

    // Параметры запроса
    Парам = Новый Структура(
        "НачалоПериода, КонецПериода, Организация, Контрагент, Менеджер, РежимСравнения",
        НачалоПериода, КонецПериода, Организация, Контрагент, Менеджер, РежимСравненияПериодов);

    ИдентификаторФонЗадания = ДлительныеОперации.ВыполнитьВФоне(
        "ДКП_ОбщийМодуль.ПолучитьДанныеДашборда", Парам, , Истина).ИдентификаторЗадания;

    ПодключитьОбработчикОжидания("ПроверкаФона", 1);
КонецПроцедуры

&НаКлиенте
Процедура ПроверкаФона()
    Сост = ДлительныеОперации.СостояниеЗадания(ИдентификаторФонЗадания);
    Если Сост.Статус <> "Выполняется" Тогда
        ОтключитьОбработчикОжидания("ПроверкаФона");
        Элементы.ГруппаИндикаторЗагрузки.Видимость = Ложь;

        Если Сост.Статус = "Выполнено" Тогда
            ДанныеДашборда = ПолучитьИзВременногоХранилища(Сост.АдресРезультата);
            ОтобразитьДанные();
        Иначе
            Сообщить("Не удалось получить данные: " + Сост.Статус);
        КонецЕсли;
    КонецЕсли;
КонецПроцедуры

#КонецОбласти

////////////////////////////////////////////////////////////////////////////////
#Область 🖼️  Отрисовка / распределение данных по элементам формы

&НаКлиенте
Процедура ОтобразитьДанные()
    Если ДанныеДашборда = Неопределено Тогда Возврат; КонецЕсли;

    // Блок «Продажи»
    Если ДанныеДашборда.Свойство("Продажи") Тогда
        Прод = ДанныеДашборда.Продажи;
        Элементы.ИндикаторВыручка.Заголовок       = Формат(Прод.Выручка, "ЧДЦ=2; ЧРГ=' '");
        Элементы.ИндикаторВыручкаГлавная.Заголовок = Элементы.ИндикаторВыручка.Заголовок;
        // ... здесь заполняем остальные элементы из блока "Продажи"
		// Например, средний чек, количество продаж, график и таблицы
		РезультатПродажи.Очистить();
		КомпоновщикМакета = Новый КомпоновщикМакетаКомпоновкиДанных;
		// ... код для вывода графика и таблиц с использованием Прод.Таблица
    КонецЕсли;

    // Блок «Взаиморасчеты»
    Если ДанныеДашборда.Свойство("Взаиморасчеты") Тогда
        Взаим = ДанныеДашборда.Взаиморасчеты;
		// Элементы.ИндикаторДебиторка.Заголовок = Формат(Взаим.ДебиторскаяЗадолженность, ...);
		// ... и так далее для всех блоков
    КонецЕсли;
	
	// И так далее для "Прибыльность", "Запасы", "ДенежныеПотоки"

КонецПроцедуры

#КонецОбласти

////////////////////////////////////////////////////////////////////////////////
#Область 🛠️  Вспомогательные процедуры

&НаКлиенте
Процедура УстановитьПериодПоШаблону()
    // Заменяет длинные IF‑else из исходника
    ДатаСеанс = ТекущаяДатаСеанса();
    Выб = БыстрыйПериод;

    НачалоПериода = ?(Выб = "Сегодня",        НачалоДня(ДатаСеанс),
                    ?(Выб = "Вчера",          НачалоДня(ДатаСеанс - 86400),
                    ?(Выб = "НеделяТекущая", НачалоНедели(ДатаСеанс),
                    ?(Выб = "НеделяПрошлая", НачалоНедели(ДатаСеанс - 604800),
                    ?(Выб = "МесяцТекущий",  НачалоМесяца(ДатаСеанс),
                    ?(Выб = "МесяцПрошлый",  НачалоМесяца(ДобавитьМесяц(ДатаСеанс, -1)),
                    НачалоПериода))))));

    КонецПериода  = ?(Выб В ("Сегодня", "НеделяТекущая", "МесяцТекущий"), КонецДня(ДатаСеанс),
                    ?(Выб = "Вчера",          КонецДня(ДатаСеанс - 86400),
                    ?(Выб = "НеделяПрошлая",  КонецНедели(ДатаСеанс - 604800),
                    ?(Выб = "МесяцПрошлый",  КонецМесяца(ДобавитьМесяц(ДатаСеанс, -1)),
                    КонецПериода))));
КонецПроцедуры

#КонецОбласти
